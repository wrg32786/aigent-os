// Capsule resume is outside F017. The fixture keeps that required runner suite
// healthy with four null-capsule cases while the caddy vectors stay isolated.
export function newestValidCapsule() {
  return null;
}
