const constAlias = unsafeRawValue;
let letAlias = unsafeRawValue;
const { unsafeRawValue: destructuredAlias } = source;
target.value = unsafeRawValue;
function escapeRawValue() { return unsafeRawValue; }
sink(unsafeRawValue);
