#!/usr/bin/env node
// Compatibility hook only. The context-pressure self-refresh reflex (60%/75%
// nudge, CAPSULE_VERB_AUTOFIRE) and the refresh-cycle tower it drove have been
// retired — settings.json may still name this file as a PreToolUse hook until
// installs are regenerated; this stub does nothing when invoked.
process.exit(0);
